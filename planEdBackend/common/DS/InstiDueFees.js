class StudentFees {
  set bId(bId) {
    this.bId = bId;
  }
  set fee(fee) {
    this.fee = fee;
  }
  set discount(discount) {
    this.discount = discount;
  }
  set scholarship(scholarship) {
    this.scholarship = scholarship;
  }

  set fine(fine) {
    this.fine = fine;
  }
  set totalFee(totalFee) {
    this.totalFee = totalFee;
  }

  set dueDt(dueDt) {
    this.dueDt = dueDt;
  }
  set paidDt(paidDt) {
    this.paidDt = paidDt;
  }
  set paidMethod(paidMethod) {
    this.paidMethod = paidMethod;
  }

  set isPaid(isPaid) {
    this.isPaid = isPaid;
  }

  set txnId(txnId) {
    this.txnId = txnId;
  }
}
